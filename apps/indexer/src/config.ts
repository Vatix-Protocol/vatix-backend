/**
 * Indexer config — thin re-export of the shared config loader.
 */
export type { IndexerConfig } from "../../../packages/shared/src/config.js";
export { loadIndexerConfig as loadConfig } from "../../../packages/shared/src/config.js";
